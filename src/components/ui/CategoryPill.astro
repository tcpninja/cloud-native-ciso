---
interface Props {
  category: string;
}

const { category } = Astro.props;
---

<span class="inline-block bg-[var(--color-primary)] text-white text-sm font-medium px-3 py-1 rounded-full transition-colors hover:bg-[var(--color-primary-dark)]">
  {category}
</span>