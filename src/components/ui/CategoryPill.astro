---
interface Props {
  category: string;
}

const { category } = Astro.props;
---

<span
  class="inline-block text-xs font-medium tracking-wide uppercase px-3 py-1 rounded-full 
         bg-primary text-white hover:bg-primary-light transition-colors duration-200"
>
  {category}
</span>
